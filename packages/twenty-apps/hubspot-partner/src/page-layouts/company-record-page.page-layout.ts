// NOTE: The ZoomInfo tab is added to the DEFAULT Company Layout via post-install,
// not via definePageLayout (which creates a separate layout that Twenty doesn't show).
// The post-install logic function handles adding the tab after each install.
// See: src/logic-functions/post-install.ts

export {};
